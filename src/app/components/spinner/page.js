import Spinner1 from '@/Components/Spinner/Part1/Spinner1';

export const metadata = {
    title: 'Spinner Components | Navigate UI',
    description: 'Spinner tailwind components'
};

function page() {
    return (
        <div>
            <Spinner1 />
        </div>
    );
}

export default page;
